export const STATUS = {
  EXPIRED: "expired",
  LIVE: "live"
};

export const REDIRECT_URL = "https://example.com/";

const HUNDRED_SECONDS = 1000 * 60 * 60;
export const THREASHOLD = HUNDRED_SECONDS;
export const ONE_DAY = 1000 * 60 * 60 * 24;
export const CLEANER_INTERVAL = 1000 * 15;

export const COLORS = {
  BACKGROUND: '#f0f0f0',
  SIDE_PANEL: '#eff3de',
  DARK_GREEN: '#4d8076'
}

export const STORAGE_KEYS = {
  BLOCK_MAP: 'soursop_extension_001_block_map'
}

export const URL_PATTERN = /https?:\/\/[^\s/$.?#].[^\s]*/;
export const DEFAULT_THRESHOLD = 60;
export const DEFAULT_ID = 0;
